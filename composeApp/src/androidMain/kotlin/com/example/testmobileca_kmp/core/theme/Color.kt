package com.example.testmobileca_kmp.core.theme

import androidx.compose.ui.graphics.Color

object AppColors {
    // Background
    val backgroundScreen = Color(0xFFF2F2F7) // iOS systemGroupedBackground equivalent

    // Text
    val bankAccountTitle = Color(0xFF3C3C43)
    val bankAccountAmount = Color(0xFF1C1C1E)
    val accountTitle = Color(0xFF3C3C43) // Consistent with bank title

    // Section
    val sectionHeader = Color(0xFF6E6E73) // Gray for section headers

    // CA Brand
    val caGreen = Color(0xFF009F4D)
    val otherBankGray = Color(0xFF8E8E93)

    // States
    val errorRed = Color(0xFFFF3B30)

    // Surface
    val cardBackground = Color.White
    val divider = Color(0xFFE5E5EA)
}
